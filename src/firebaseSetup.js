

firebase.initializeApp(firebaseConfig)

function init() {
    return new Promise((resolve, reject) => {
        try {

        }
        catch (error) {
            reject(error)
        }
    })
}

export default {
    init
}